# Provisioned dashboard provider (CIDRX-045) — loads every JSON dashboard
# from /var/lib/grafana/dashboards (mounted from grafana/dashboards/).
apiVersion: 1

providers:
  - name: cidrx
    folder: ""
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
